# Synthetic boundary fixtures

Tests generate inert text, archive, and image samples in temporary directories. Binary samples
are intentionally not committed; every value and image is fictional and created by test helpers.
